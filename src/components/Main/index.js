import ContextMenu from "../ComponentToTest/ContextMenu";
import React from "react";

const Main = () => {
  return (
    <div className="bg-gray-50 h-screen flex-1 flex items-center justify-center font-lato sm:text-xs md:text-sm lg:text-base relative">
      <ContextMenu />
    </div>
  );
};

export default Main;
